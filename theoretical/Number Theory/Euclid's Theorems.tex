\begin{itemize}
    \item \textbf{Euclide's Lemma: }If a prime $p$ divides the product of two integers $ab$, then $p$ must divide at least one of those integers $a$ or $b$.
    \item \textbf{Bézout's identity: }$ax +by = $gcd$(a,b)$.
\end{itemize}